version: '3.8'
services:
  mysql:
    image: mysql:5.7
    cap_add:
      - SYS_NICE
    ports:
      - '3306:3306'
    environment:
      - MYSQL_ROOT_PASSWORD=default_password
      - MYSQL_DATABASE=checkpoint
    volumes:
      - mysql:/var/lib/mysql
volumes:
  mysql:
    driver: local
