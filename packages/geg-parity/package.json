{
  "name": "@snapshot-labs/geg-parity",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Gate 0: replays the generalised-el-gamal protocol's canonical vector corpus against the pinned @shutter-network/urban-verified-crypto build. Tests only — ships no code.",
  "scripts": {
    "test": "jest",
    "sync-vectors": "node scripts/sync-geg-vectors.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@shutter-network/urban-verified-crypto": "0.3.0"
  },
  "devDependencies": {
    "@ethersproject/keccak256": "^5.7.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.3"
  }
}
